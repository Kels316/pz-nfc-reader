# PZTrack NFC Reader — Python dependencies
# Install inside the venv created by install.sh, or manually:
#   pip install -r requirements.txt

# RC522 NFC/RFID reader (SPI)
mfrc522>=0.0.7

# OLED display driver (SSD1306/SSD1315 via I²C)
luma.oled>=3.12.0

# Image rendering for OLED
Pillow>=10.0.0

# HTTP client for PZTrack REST API
requests>=2.31.0

# PostgreSQL driver (binary wheel — no libpq-dev build required on the Pi)
psycopg2-binary>=2.9.0

# GPIO button support (pre-installed on Raspberry Pi OS, listed here for completeness)
gpiozero>=2.0
